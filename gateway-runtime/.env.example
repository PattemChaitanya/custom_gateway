# Injected by instance_manager.py at container startup
ACCOUNT_ID=acc_123
GATEWAY_PORT=5000
REDIS_URL=redis://redis:6379/0
CONTROL_PLANE_URL=http://backend:8000
ACCOUNT_SECRET=super-secret-token

# Optional: where to persist SQLite DB inside the container
SQLITE_PATH=/data/gateway.db
